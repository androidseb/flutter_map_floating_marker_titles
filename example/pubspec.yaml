name: flutter_floating_map_marker_titles_demo
description: A new Flutter project.

# The following line prevents the package from being accidentally published to
# pub.dev using `pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  flutter_floating_map_marker_titles_core: ^0.0.1+1
  flutter_map_floating_marker_titles: ^0.0.1+1
  google_maps_flutter_floating_marker_titles: ^0.0.1+1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - lib/assets/images/marker.png
